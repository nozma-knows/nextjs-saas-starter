import { Flex, Text } from '@chakra-ui/react';
import { FC } from 'react';

interface Props {}

const Usage: FC<Props> = () => {
  return (
    <Flex>
      <Text>Usage</Text>
    </Flex>
  );
};

export default Usage;
